function getURLParameter(name) {
  return decodeURIComponent((new RegExp('[?|&]' + name + '=' + '([^&;]+?)(&|#|;|$)').exec(location.search)||[,""])[1].replace(/\+/g, '%20'))||null
}
if(getURLParameter('n')) {
  if(getURLParameter('n') == "Clay%20Hanson") {
    return 1;
    document.writeln("1");
  }
}
